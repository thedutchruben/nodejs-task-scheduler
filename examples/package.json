{
  "name": "nodejs-task-scheduler-examples",
  "version": "1.0.0",
  "description": "Usage examples for nodejs-task-scheduler",
  "private": true,
  "scripts": {
    "basic": "ts-node basic-usage.ts",
    "email-service": "ts-node email-service.ts",
    "multi-node": "ts-node multi-node-setup.ts",
    "decorators": "ts-node decorator-examples.ts",
    "dev": "ts-node-dev --respawn --transpile-only",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "nodejs-task-scheduler": "file:.."
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.2"
  }
}